schema
    route
        id string
        source string
        destination string
        path string[]
        conflicts string[]
        point_positions string[]
        length_cm float
        train_id string
    end

    route_point
        position string
    end
end
