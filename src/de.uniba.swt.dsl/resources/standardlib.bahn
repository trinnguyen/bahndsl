schema
    route
        source string
        destination string
        path string[]
        conflicts string[]
        point_positions string[]
        train_id string
        length_cm float
    end

    route_point
        position string
    end
end